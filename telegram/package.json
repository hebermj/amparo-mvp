{
  "name": "amparo-telegram",
  "version": "2.0.0",
  "description": "Amparo MVP — Telegram Bot Gateway + Sender (substitui WhatsApp Business API)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --passWithNoTests",
    "syntax": "node --check src/index.js && find src -name '*.js' -exec node --check {} \\;"
  },
  "dependencies": {
    "amqplib": "^0.10.5",
    "dotenv": "^16.4.7",
    "node-fetch": "^2.7.0",
    "prom-client": "^15.1.3",
    "telegraf": "^4.16.3",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
